#include <iostream>

using namespace std;

int main() {
	int NX, NY, K, cot=0,i,j,nx,ny;
	cin >> NX >> NY >> K;
	if (NX * NY < K) {
		cout << '0';
		return 0;
	}
	ny = NY; 
	nx = NX-1;
	int y = 0, x = 1, sw = 1;
	for (i = 1; i <= ny; i++) {
		for (j = 1; j <= nx; j++) {

		}
	}


	return 0;
}